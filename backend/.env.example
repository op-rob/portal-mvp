# Database Configuration
DATABASE_URL=postgresql://user:password@localhost:5432/ownerpulse
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=password
DB_NAME=ownerpulse

# Auth0 Configuration
AUTH0_DOMAIN=your-domain.auth0.com
AUTH0_AUDIENCE=your-api-audience
AUTH0_CLIENT_ID=your-client-id
AUTH0_CLIENT_SECRET=your-client-secret

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key
JWT_EXPIRES_IN=24h

# Application Configuration
PORT=3001
NODE_ENV=development
FRONTEND_URL=http://localhost:3000

# Development Setup Notes:
# 1. Create an Auth0 account and application (Single Page Application)
# 2. Create an Auth0 API with identifier matching AUTH0_AUDIENCE
# 3. Enable RS256 algorithm for JWT signing
# 4. Add custom claims in Auth0 Actions if needed:
#    - https://ownerpulse.com/roles
#    - https://ownerpulse.com/permissions
